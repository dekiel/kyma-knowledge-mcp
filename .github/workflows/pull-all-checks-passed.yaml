name: All Checks Passed

permissions:
  contents: read

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review
      - edited
      - labeled
      - milestoned

jobs:
  all-checks:
    runs-on: ubuntu-latest
    permissions:
      checks: read
      contents: read
    steps:
      - name: Check if all checks passed
        uses: wechuli/allcheckspassed@644b5ba69cbde521899ba3093e41e28c49a1f8ab # v2.3.0
        with:
          delay: "1"
          retries: "10"
          polling_interval: "1"
